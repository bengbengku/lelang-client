import { createStyles, rem } from "@mantine/core";

export const introStyles = createStyles((theme) => ({
  intro: {
    width: "100%",
    paddingTop: "5px !important",
    margin: "0 auto !important",
  },
  button: {
    padding: "1.25rem 0",
  },
}));
